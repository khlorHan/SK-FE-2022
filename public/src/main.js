/* eslint-disable */

import { App } from './App.js';

// v17 API
const container = document.getElementById('app');
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  container
);
